import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    primaryColor: {color: '#1e90ff'},
    backgroundColor: {backgroundColor: '#f5f5f5'},
    bodyFontSize: {fontSize: 16},
});